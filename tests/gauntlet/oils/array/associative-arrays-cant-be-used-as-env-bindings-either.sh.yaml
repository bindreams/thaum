name: Associative arrays can't be used as env bindings either
dialect: bash
source: oils:spec/array#associative-arrays-cant-be-used-as-env-bindings-either
status: 2
stdout: ''
setup: |-
  #!/bin/sh
  cat > printenv.py <<'HELPER'
  #!/usr/bin/env python3
  import os, sys
  for name in sys.argv[1:]:
      print(os.environ.get(name))
  HELPER
  chmod +x printenv.py
---
A=a B=([k]=v) printenv.py A B
