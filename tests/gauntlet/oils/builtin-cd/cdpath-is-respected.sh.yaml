name: CDPATH is respected
dialect: bash
source: oils:spec/builtin-cd#cdpath-is-respected
stdout: |
  /tmp/spam/foo
  status=0
  /tmp/spam/foo
---

mkdir -p /tmp/spam/foo /tmp/eggs/foo

CDPATH='/tmp/spam:/tmp/eggs'

cd foo
echo status=$?
pwd


# doesn't print the dir
