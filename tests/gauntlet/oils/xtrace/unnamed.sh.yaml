name: '[[ ]]'
dialect: bash
source: oils:spec/xtrace#unnamed
stdout: ''
stderr: |
  + dir=/
  + [[ -d $dir ]]
  + (( a = 42 ))
---
case $SH in dash|mksh) exit ;; esac

set -x

dir=/
if [[ -d $dir ]]; then
  (( a = 42 ))
fi
