name: ${mystr[@]} and ${mystr[*]} disallowed with strict_array
dialect: bash
source: oils:spec/array#mystr-and-mystr-disallowed-with-strictarray
status: 0
stdout: |
  status=1
  status=1
---

$SH -c 'shopt -s strict_array; s="abc"; echo ${s[@]}'
echo status=$?

$SH -c 'shopt -s strict_array; s="abc"; echo ${s[*]}'
echo status=$?
