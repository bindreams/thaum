name: kill -L also shows signals
dialect: bash
source: oils:spec/builtin-kill#kill--l-also-shows-signals
stdout: |-
  0
  ## N-I mksh/dash/zsh STDOUT:
---
case $SH in mksh|dash|zsh) exit ;; esac

builtin kill -L | grep HUP > /dev/null
echo $?
