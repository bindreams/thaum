name: operator/operand ambiguity with ]
dialect: bash
source: oils:spec/builtin-bracket#operatoroperand-ambiguity-with
stdout: |
  status=0
---
# bash parses this as '-z' AND ']', which is true.  It's a syntax error in
# dash/mksh.
[ -z -a ] ]
echo status=$?
