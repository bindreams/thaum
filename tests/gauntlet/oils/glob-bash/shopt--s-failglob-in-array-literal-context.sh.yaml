name: shopt -s failglob in array literal context
dialect: bash
source: oils:spec/glob-bash#shopt--s-failglob-in-array-literal-context
stdout: |
  *.ZZ
  status=1
---
myarr=(*.ZZ)
echo "${myarr[@]}"
shopt -s failglob
myarr=(*.ZZ)
echo status=$?
