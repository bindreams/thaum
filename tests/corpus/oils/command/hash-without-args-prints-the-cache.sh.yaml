name: hash without args prints the cache
dialect: bash
source: oils:spec/command#hash-without-args-prints-the-cache
stdout: "/usr/bin/whoami\nstatus=0\n"
---
whoami >/dev/null
hash
echo status=$?

# bash uses a weird table.  Although we could use TSV2.
