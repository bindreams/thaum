name: kill -s TERM specifies the signal name
dialect: bash
source: oils:spec/builtin-kill#kill--s-term-specifies-the-signal-name
stdout: |-
  kill=0
  wait=143
---
sleep 0.1 &
pid=$!
kill -s TERM $pid
echo kill=$?

wait $pid
echo wait=$?
