name: With extglob on, !($str) on the left or right of == has different meanings
dialect: bash
source: 
  oils:spec/extglob-match#with-extglob-on-str-on-the-left-or-right-of-has-different-meanings
stdout: |
  TRUE
---
shopt -s extglob
str='x'
[[ 1 == !($str) ]]  && echo TRUE   # glob match
