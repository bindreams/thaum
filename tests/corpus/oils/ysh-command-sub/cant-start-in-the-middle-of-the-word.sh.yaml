name: "@() can't start in the middle of the word"
dialect: bash
source: oils:spec/ysh-command-sub#cant-start-in-the-middle-of-the-word
status: 1
stdout: "\n"
---
shopt -s extglob  # this makes it match in bash
shopt -s oil:upgrade

# This is currently a RUNTIME error when simple_word_eval is on

touch foo.py
echo f@(*.py)
