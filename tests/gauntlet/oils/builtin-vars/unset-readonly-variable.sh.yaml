name: Unset readonly variable
dialect: bash
source: oils:spec/builtin-vars#unset-readonly-variable
status: 0
stdout: |
  status=1
---
# dash and zsh abort the whole program.   OSH doesn't?
readonly R=foo
unset R
echo status=$?
