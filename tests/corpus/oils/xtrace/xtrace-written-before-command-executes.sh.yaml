name: xtrace written before command executes
dialect: bash
source: oils:spec/xtrace#xtrace-written-before-command-executes
stdout: ''
stderr: |-
  + echo one
  one
  + echo two
  two
  ## OK mksh STDERR:
  # mksh traces redirects!
  + >&2 
  + echo one
  one
  + >&2 
  + echo two
  two
---
set -x
echo one >&2
echo two >&2
