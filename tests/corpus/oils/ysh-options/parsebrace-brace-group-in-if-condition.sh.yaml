disabled: true
name: 'parse_brace: brace group in if condition'
dialect: bash
source: oils:spec/ysh-options#parsebrace-brace-group-in-if-condition
stdout: |
  one
  two
  three
---

# strict_errexit would make this a RUNTIME error
shopt -s parse_brace
if { echo one; echo two } {
  echo three
}
