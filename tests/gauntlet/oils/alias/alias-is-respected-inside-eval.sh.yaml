name: Alias is respected inside eval
dialect: bash
source: oils:spec/alias#alias-is-respected-inside-eval
stdout: |
  hello inside
  hello outside
---
shopt -s expand_aliases
eval "alias sayhi='echo hello'
sayhi inside"
sayhi outside
