name: 'dynamic arith varname: read'
dialect: bash
source: oils:spec/ble-idioms#dynamic-arith-varname-read
stdout: "x=12 y=34\n"
---

vec2_load() {
  local this=$1
  x=$(( ${this}_x ))
  : $(( y = ${this}_y ))
}
a_x=12 a_y=34
vec2_load a
echo x=$x y=$y
