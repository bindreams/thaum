disabled: true
name: Can read from ENV Obj
dialect: bash
source: oils:spec/ysh-env#can-read-from-env-obj
stdout: "(Str)   \"Obj\"\nok\n"
---
shopt -s ysh:upgrade

pp test_ (type(ENV))
#pp test_ (ENV)

# Set by the spec test harness

if (ENV.SH ~~ '*osh') {
  echo ok
}
#echo SH=$[ENV.SH]
