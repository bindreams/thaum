name: '[[ over multiple lines is OK'
dialect: bash
source: oils:spec/dbracket#over-multiple-lines-is-ok
status: 0
stdout: |
  true
---
# Hm it seems you can't split anywhere?
[[ foo == foo
&& bar == bar
]] && echo true
