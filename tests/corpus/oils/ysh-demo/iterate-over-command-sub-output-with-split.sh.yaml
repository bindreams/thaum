disabled: true
name: Iterate over command sub output with split()
dialect: bash
source: oils:spec/ysh-demo#iterate-over-command-sub-output-with-split
stdout: |-
  1
  one
  2
  two
  ___
  1 one
  2 two
---
shopt -s ysh:upgrade

output=$(echo '1 one'; echo '2 two')

for x in @[split(output)]; do
  write -- $x
done

echo ___

# Now change IFS.  split() is affected.
IFS=$'\n'
for x in @[split(output)]; do
  write -- $x
done
