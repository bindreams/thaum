name: xtrace with newlines
dialect: bash
source: oils:spec/xtrace#xtrace-with-newlines
stdout: |
  [
  ]
  ## STDERR: 
  + echo $'[\n]'
---
# bash and dash trace this badly.  They print literal newlines, which I don't
# want.
set -x
echo $'[\n]'
# bash has ugly output that spans lines
