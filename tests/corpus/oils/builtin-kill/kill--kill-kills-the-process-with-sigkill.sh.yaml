name: kill -KILL kills the process with SIGKILL
dialect: bash
source: oils:spec/builtin-kill#kill--kill-kills-the-process-with-sigkill
stdout: |-
  kill=0
  wait=137
---
sleep 0.1 & 
pid=$!
kill -KILL $pid 
echo kill=$?

wait $pid
echo wait=$?  # 137 is 128 + SIGKILL
