name: globstar is off -> ** is treated like *
dialect: bash
source: oils:spec/globstar#globstar-is-off---is-treated-like
stdout: |-
  c/leaf.md
---
case $SH in zsh) exit ;; esac

shopt -u globstar

mkdir -p c/subdir
touch {leaf.md,c/leaf.md,c/subdir/leaf.md}

echo **/*.* | sort
