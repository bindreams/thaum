name: pattern $f(x)  -- regression
dialect: bash
source: oils:spec/regex#pattern-fx----regression
stdout: "status=0\nstatus=1\n"
---
f=fff
[[ fffx =~ $f(x) ]]
echo status=$?
[[ ffx =~ $f(x) ]]
echo status=$?
