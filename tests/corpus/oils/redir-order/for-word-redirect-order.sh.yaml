name: for word + redirect order
dialect: bash
source: oils:spec/redir-order#for-word-redirect-order
stdout: |-
  world
---

echo hello > OSCFLAGS
for x in `cat OSCFLAGS` world; do
  echo $x
done > OSCFLAGS
cat OSCFLAGS
