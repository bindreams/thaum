name: Alias that is && || ;
dialect: bash
source: oils:spec/alias#alias-that-is
stdout: |
  one
  two
  1
  four
---
shopt -s expand_aliases
alias t1='echo one && echo two && echo 3 | wc -l;
echo four'
t1
