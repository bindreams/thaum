name: array with empty string
dialect: bash
source: oils:spec/array#array-with-empty-string
stdout: "['']\n"
setup: |
  #!/bin/sh
  cat > argv.py <<'HELPER'
  #!/usr/bin/env python3
  import sys
  print(sys.argv[1:])
  HELPER
  chmod +x argv.py

---
empty=('')
argv.py "${empty[@]}"
