name: procs are in same namespace as variables
dialect: bash
source: oils:spec/ysh-proc#procs-are-in-same-namespace-as-variables
stdout: "myproc is a Proc\n"
---
shopt --set parse_proc parse_ysh_expr_sub

proc myproc {
  echo hi
}

echo "myproc is a $[type(myproc)]"
