name: -k for sticky bit
dialect: bash
source: oils:spec/builtin-bracket#k-for-sticky-bit
stdout: "status=0\nstatus=1\n"
---
# not isolated: /tmp usually has sticky bit on
# https://en.wikipedia.org/wiki/Sticky_bit

test -k /tmp
echo status=$?

test -k /bin
echo status=$?
