name: process sub in background &
dialect: bash
source: oils:spec/process-sub#process-sub-in-background
stdout: "1\n2\n3\nsync\nfork\n"
---

cat <(seq 3; sleep 0.1) & wait

echo sync

# This one escapes, and the shell should still exit
cat <(sleep 0.1) &

echo fork
