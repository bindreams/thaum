name: errexit double guard
dialect: bash
source: oils:spec/errexit#errexit-double-guard
status: 1
stdout: "true\n"
---
# OSH bug fix.  ErrExit needs a counter, not a boolean.
set -o errexit
if { ! false; false; true; } then
  echo true
fi
false
echo done
