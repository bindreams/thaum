name: ( ) ! -a -o with system version of [
dialect: bash
source: oils:spec/builtin-bracket#a--o-with-system-version-of
stdout: |
  true
---
command [ --version
command [ -z '' -a '(' ! -z x ')' ] && echo true
