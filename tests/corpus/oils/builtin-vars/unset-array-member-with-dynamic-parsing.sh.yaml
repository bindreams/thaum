name: Unset array member with dynamic parsing
dialect: bash
source: oils:spec/builtin-vars#unset-array-member-with-dynamic-parsing
stdout: |
  status=0
  x z len=2
---

i=1
a=(w x y z)
unset 'a[ i - 1 ]' a[i+1]  # note: can't have space between a and [
echo status=$?
echo "${a[@]}" len="${#a[@]}"
