name: declare -l -u
dialect: bash
source: oils:spec/type-compat#declare--l--u
stdout: |-
  foo
  FOO
---

declare -l lower=FOO
declare -u upper=foo

echo $lower
echo $upper

# other:
# -t trace
# -I inherit attributes
