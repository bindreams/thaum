name: printf %b unicode escapes
dialect: bash
source: oils:spec/builtin-printf#printf-b-unicode-escapes
stdout: |
  [\u03bc]
  [μ]
---

printf '[%s]\n' '\u03bc'  # escapes not evaluated
printf '[%b]\n' '\u03bc'  # YES, escapes evaluated
