name: ${!ref-default}
dialect: bash
source: oils:spec/var-ref#ref-default
stdout: |
  x=default
  x=
  x=foo
---
ref=x
echo x=${!ref-default}

x=''
echo x=${!ref-default}

x=foo
echo x=${!ref-default}
