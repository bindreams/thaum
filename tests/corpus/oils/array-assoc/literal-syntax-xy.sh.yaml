name: Literal syntax ([x]=y)
dialect: bash
source: oils:spec/array-assoc#literal-syntax-xy
stdout: "b\nbar\nc\n"
---
declare -A a
a=([aa]=b [foo]=bar ['a+1']=c)
echo ${a["aa"]}
echo ${a["foo"]}
echo ${a["a+1"]}
