name: shopt -s strict:all || true while errexit is on
dialect: bash
source: oils:spec/errexit#shopt--s-strictall-true-while-errexit-is-on
status: 1
stdout: |-
  one
---
set -o errexit
shopt -s strict:all || true
echo one
false  # fail
echo two
