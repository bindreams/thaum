name: 'var ref: 1, @, *'
dialect: bash
source: oils:spec/var-ref#var-ref-1
stdout: "['x']\n['x', 'y']\n['x y']\n"
---
set -- x y
ref=1; argv.py "${!ref}"
ref=@; argv.py "${!ref}"
ref=*; argv.py "${!ref}"  # maybe_decay_array bug?
