name: '"${a[@]}" and "${a[*]}"'
dialect: bash
source: oils:spec/array-basic#a-and-a
stdout: |
  ['1', '2 3', '1 2 3']
setup: |-
  #!/bin/sh
  cat > argv.py <<'HELPER'
  #!/usr/bin/env python3
  import sys
  print(sys.argv[1:])
  HELPER
  chmod +x argv.py
---
a=(1 '2 3')
argv.py "${a[@]}" "${a[*]}"
