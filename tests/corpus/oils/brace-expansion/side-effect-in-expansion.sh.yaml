name: Side effect in expansion
dialect: bash
source: oils:spec/brace-expansion#side-effect-in-expansion
stdout: "a-0 b-1 c-2\n"
---
# bash is the only one that does it first.  I guess since this is
# non-POSIX anyway, follow bash?
i=0
echo {a,b,c}-$((i++))
