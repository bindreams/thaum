name: PS4 with unterminated $(
dialect: bash
source: oils:spec/xtrace#ps4-with-unterminated
stdout: |-
  one
  status=0
---
# osh shows inline error; maybe fail like dash/mksh?
x=1
PS4='+$(x'
set -o xtrace
echo one
echo status=$?
# mksh and dash both fail.  bash prints errors to stderr.
