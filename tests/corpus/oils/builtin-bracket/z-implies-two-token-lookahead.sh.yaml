name: -z '>' implies two token lookahead
dialect: bash
source: oils:spec/builtin-bracket#z-implies-two-token-lookahead
stdout: "true\nfalse\ntrue\n"
---
[ -z ] && echo true  # -z is operand
[ -z '>' ] || echo false  # -z is operator
[ -z '>' -- ] && echo true  # -z is operand
