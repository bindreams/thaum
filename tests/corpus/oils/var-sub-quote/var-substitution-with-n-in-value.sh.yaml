name: Var substitution with \n in value
dialect: bash
source: oils:spec/var-sub-quote#var-substitution-with-n-in-value
stdout: |-
  a\nb
  c\nd
  e\nf
---
echo "${var-a\nb}"
echo "${var:-c\nd}"
var=val
echo "${var:+e\nf}"
