disabled: true
name: no_xtrace_osh doesn't show [[ ]] etc.
dialect: bash
source: oils:spec/ysh-xtrace#noxtraceosh-doesnt-show-etc
stdout: ''
stderr: |-
  . builtin cd /
---
shopt -s ysh:upgrade
set -x

dir=/
if [[ -d $dir ]]; then
  (( a = 42 ))
fi
cd /
