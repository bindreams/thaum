name: One 'echo' in first pipeline part - why does bash behave differently from 
  case above?
dialect: bash
source: 
  oils:spec/builtin-trap-bash#one-echo-in-first-pipeline-part---why-does-bash-behave-differently-from-case-above
stdout: |2-
    LINENO=6
    LINENO=7
  pipeline
    LINENO=8
  ok
---

# TODO: bash runs the trap 3 times, and osh only twice.  I don't see why.  Is
# it because Process::Run() does trap_state.ClearForSubProgram()?  Probably
#echo top PID=$$ BASHPID=$BASHPID
#shopt -s lastpipe

debuglog() {
  #echo "  PID=$$ BASHPID=$BASHPID LINENO=$1"
  #echo "  LINENO=$1 $BASH_COMMAND"
  # LINENO=6 echo pipeline
  # LINENO=7 cat
  echo "  LINENO=$1"
}
trap 'debuglog $LINENO' DEBUG

echo pipeline \
  | cat
echo ok
