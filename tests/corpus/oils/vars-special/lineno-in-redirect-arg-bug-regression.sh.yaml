name: $LINENO in redirect arg (bug regression)
dialect: bash
source: oils:spec/vars-special#lineno-in-redirect-arg-bug-regression
---
filename=$TMP/lineno_regression3
rm -f $filename
echo x > $TMP/lineno_regression$LINENO
test -f $filename && echo written
echo $LINENO
written
5
