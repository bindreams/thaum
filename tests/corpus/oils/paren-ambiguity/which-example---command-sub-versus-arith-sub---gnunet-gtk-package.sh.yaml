name: $((which example - command sub versus arith sub - gnunet-gtk package
dialect: bash
source: 
  oils:spec/paren-ambiguity#which-example---command-sub-versus-arith-sub---gnunet-gtk-package
stdout: "bye\n"
---

        gtk_update_icon_cache_bin="$((which gtk-update-icon-cache ||
echo /opt/gnome/bin/gtk-update-icon-cache)2>/dev/null)"

echo bye
