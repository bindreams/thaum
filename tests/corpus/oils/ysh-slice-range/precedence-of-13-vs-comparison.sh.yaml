name: precedence of 1:3 vs comparison
dialect: bash
source: oils:spec/ysh-slice-range#precedence-of-13-vs-comparison
stdout: "(Range 1 ..< 3)\n"
---

# This test exposed nondeterminism in CPython itself!  Gah.  Is this because of
# the hashing?
# Python xrange objects probably shouldn't even be comparable!
#
# = 1..3 < 1..4
# >>> xrange(1,3)  < xrange(1,4)
# False
# >>> xrange(1,3)  < xrange(1,4)
# True

= 1..<3
