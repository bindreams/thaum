name: Start background pipeline, wait %job_spec
dialect: bash
source: oils:spec/background#start-background-pipeline-wait-jobspec
stdout: "status=0\nstatus=99\n"
---
case $SH in mksh) exit ;; esac  # flakiness?

echo hi | { exit 99; } &
echo status=$?
wait %1
echo status=$?
