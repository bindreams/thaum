name: special builtins are called out
dialect: bash
source: oils:spec/builtin-type#special-builtins-are-called-out
stdout: |-
  cd is a shell builtin
  eval is a special shell builtin
  : is a special shell builtin
  true is a shell builtin

  export is a special shell builtin
---
type cd
type eval
type :
type true

echo
type export
