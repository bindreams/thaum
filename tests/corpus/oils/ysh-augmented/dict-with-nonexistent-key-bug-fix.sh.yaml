disabled: true
name: Dict with nonexistent key (bug fix)
dialect: bash
source: oils:spec/ysh-augmented#dict-with-nonexistent-key-bug-fix
status: 3
stdout: "44\n"
---

shopt -s parse_ysh_expr_sub

var d = {x: 42}

setvar d.x += 1
setglobal d.x += 1
echo $[d.x]

setvar d.missing -= 1
setglobal d.missing -= 1
