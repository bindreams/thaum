name: 'two args: -z with = ! ( ]'
dialect: posix
source: oils:spec/builtin-bracket#two-args--z-with
---
[ -z = ]
echo status=$?
[ -z ] ]
echo status=$?
[ -z '!' ]
echo status=$?
[ -z '(' ]
echo status=$?
status=1
status=1
status=1
status=1
