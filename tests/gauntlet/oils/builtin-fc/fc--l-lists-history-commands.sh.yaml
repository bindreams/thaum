name: fc -l lists history commands
dialect: bash
source: oils:spec/builtin-fc#fc--l-lists-history-commands
stdout: |
  1	 history -r
  2	 echo 1
  3	 echo 2
  4	 echo 3
  ^D
---
printf "echo %s\n" {1..3} > tmp

echo '
HISTFILE=tmp
history -c
history -r

fc -l
' | $SH --norc -i

# match osh's behaviour of echoing ^D for EOF
case $SH in bash) echo '^D' ;; esac
