name: fatal errors continue
dialect: bash
source: oils:spec/interactive#fatal-errors-continue
status: 42
stdout: |
  one
---
# NOTE: tried here doc, but sys.stdin.isatty() fails.  Could we fake it?
$SH --rcfile /dev/null -i -c '
echo $(( 1 / 0 ))
echo one
exit 42
'
