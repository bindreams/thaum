name: source accepts/ignores --
dialect: bash
source: oils:spec/builtin-eval-source#source-acceptsignores
stdout: |
  foo
environment:
  TMP: /tmp
---
echo 'echo foo' > $TMP/foo.sh
source -- $TMP/foo.sh
