name: a[$x$y] on LHS and RHS
dialect: bash
source: oils:spec/array#axy-on-lhs-and-rhs
stdout: "foo\n1\n"
---
x=1
y=2
a[$x$y]=foo

# not allowed by OSH parsing
#echo ${a[$x$y]}

echo ${a[12]}
echo ${#a[@]}
