name: OSH can use ARGV name
dialect: bash
source: oils:spec/bugs#osh-can-use-argv-name
stdout: |
  len=2
---
case $SH in dash|ash) exit ;; esac

foo() {
  if test -v ARGV; then
    echo 'BUG local'
  fi
  ARGV=( a b )
  echo len=${#ARGV[@]}
}

if test -v ARGV; then
  echo 'BUG global'
fi
foo
