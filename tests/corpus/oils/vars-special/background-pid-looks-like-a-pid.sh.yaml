name: Background PID $! looks like a PID
dialect: bash
source: oils:spec/vars-special#background-pid-looks-like-a-pid
stdout: |-
  status=0
---
sleep 0.01 &
pid=$!
wait
echo $pid | egrep '[0-9]+' >/dev/null
echo status=$?
