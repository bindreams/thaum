name: kill -l with 128 is invalid
dialect: bash
source: oils:spec/builtin-kill#kill--l-with-128-is-invalid
stdout: |-
  invalid
  ## N-I mksh STDOUT:
  128
---
kill -l 128
if [ $? -ne 0 ]; then
    echo "invalid"
fi
