name: dirs tilde test when $HOME is exactly $PWD
dialect: bash
source: oils:spec/builtin-dirs#dirs-tilde-test-when-home-is-exactly-pwd
status: 0
stdout: |-
  ~ /
  ~ /
---
cd /
mkdir -p /tmp/oil_test
HOME=/tmp/oil_test
pushd $HOME
dirs
# zsh doesn't duplicate the stack I guess.
