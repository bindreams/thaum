name: '? matches 0 or 1'
dialect: bash
source: oils:spec/extglob-match#matches-0-or-1
stdout: |
  TRUE
  FALSE
---
[[ -- == --?(help|verbose) ]] && echo TRUE
[[ --oops == --?(help|verbose) ]] || echo FALSE
