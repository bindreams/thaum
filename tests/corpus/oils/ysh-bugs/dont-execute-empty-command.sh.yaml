disabled: true
name: don't execute empty command
dialect: bash
source: oils:spec/ysh-bugs#dont-execute-empty-command
status: 127
stdout: "type -a returned 1\n"
---

shopt --set ysh:all

set -x

try {
  type -a ''
}
echo "type -a returned $[_error.code]"

$(true)
echo nope
