name: type -a -P gives multiple files
dialect: bash
source: oils:spec/builtin-type-bash#type--a--p-gives-multiple-files
stdout: |
  _tmp/pwd
  /bin/pwd
---

touch _tmp/pwd
chmod +x _tmp/pwd
PATH="_tmp:/bin"

type -a -P pwd
