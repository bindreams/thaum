name: Fatal error inside [[ =~ ]]
dialect: bash
source: oils:spec/regex#fatal-error-inside
status: 1
stdout: ''
---

# zsh and osh are stricter than bash.  bash treats [[ like a command.

[[ a =~ $(( 1 / 0 )) ]]
echo status=$?
