name: Array coerces to string (shopt -s strict_array to disallow)
dialect: bash
source: 
  oils:spec/dbracket#array-coerces-to-string-shopt--s-strictarray-to-disallow
stdout: |-
  true
  false
---
a=('1 3' 5)
[[ '1 3 5' = "${a[@]}" ]] && echo true
[[ '1 3 4' = "${a[@]}" ]] || echo false
