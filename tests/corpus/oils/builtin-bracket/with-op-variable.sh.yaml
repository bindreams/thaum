name: '[ with op variable'
dialect: bash
source: oils:spec/builtin-bracket#with-op-variable
status: 0
stdout: |
  true
  false
---
# OK -- parsed AFTER evaluation of vars
op='='
[ a $op a ] && echo true
[ a $op b ] || echo false
