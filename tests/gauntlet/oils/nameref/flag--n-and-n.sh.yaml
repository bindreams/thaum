name: flag -n and +n
dialect: bash
source: oils:spec/nameref#flag--n-and-n
stdout: |
  ref=x
  ref=foo
  ref=bar
  ref=x
---
x=foo

ref=x

echo ref=$ref

typeset -n ref
echo ref=$ref

# mutate underlying var
x=bar
echo ref=$ref

typeset +n ref
echo ref=$ref
