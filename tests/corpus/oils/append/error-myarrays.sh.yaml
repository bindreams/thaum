name: 'error: myarray+=s'
dialect: bash
source: oils:spec/append#error-myarrays
status: 1
stdout: ''
---

# They treat this as implicit index 0.  We disallow this on the LHS, so we will
# also disallow it on the RHS.
a=(x y )
a+=z
argv.py "${a[@]}"
