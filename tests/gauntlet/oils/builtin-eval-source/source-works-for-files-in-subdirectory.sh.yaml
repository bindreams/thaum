name: source works for files in subdirectory
dialect: bash
source: oils:spec/builtin-eval-source#source-works-for-files-in-subdirectory
stdout: |
  path
---
mkdir -p dir
echo "echo path" > dir/cmd
. dir/cmd
rm dir/cmd
