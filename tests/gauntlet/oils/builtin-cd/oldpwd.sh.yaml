name: $OLDPWD
dialect: bash
source: oils:spec/builtin-cd#oldpwd
stdout: |
  old: /
  OLDPWD=/
  /
environment:
  TMP: /tmp
---
cd /
cd $TMP
echo "old: $OLDPWD"
env | grep OLDPWD  # It's EXPORTED too!
cd -
