name: declare
dialect: bash
source: oils:spec/serialize#declare
stdout: |-
  zz=$'one\ntwo'
  declare -- zz=$'one\ntwo'
---
case $SH in ash|zsh) return ;; esac  # zsh doesn't make much sense

zz=$'one\ntwo'

typeset | grep zz
typeset -p zz
