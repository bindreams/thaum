name: Simple Expr Closure
dialect: bash
source: oils:spec/ysh-closures#simple-expr-closure
stdout: "43\n"
---
shopt --set ysh:upgrade

proc my-expr (; expr) {
  echo $[io->evalExpr(expr)]
}

proc p {
  var i = 42
  my-expr [i + 1]
}

p
