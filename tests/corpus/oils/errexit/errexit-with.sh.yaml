name: errexit with { }
dialect: bash
source: oils:spec/errexit#errexit-with
status: 1
stdout: |-
  one
---
# This aborts because it's not part of an if statement.
set -o errexit
{ echo one; false; echo two; }
