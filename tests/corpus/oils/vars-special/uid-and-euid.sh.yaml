name: $UID and $EUID
dialect: bash
source: oils:spec/vars-special#uid-and-euid
stdout: |-
  status=0
---
# These are both bash-specific.
set -o errexit
echo $UID | egrep -o '[0-9]+' >/dev/null
echo $EUID | egrep -o '[0-9]+' >/dev/null
echo status=$?
