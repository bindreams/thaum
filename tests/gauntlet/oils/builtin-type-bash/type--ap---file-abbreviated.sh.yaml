name: type -ap -> file; abbreviated
dialect: bash
source: oils:spec/builtin-type-bash#type--ap---file-abbreviated
stdout: |
  /bin/date
  _tmp/date
---
touch _tmp/date
chmod +x _tmp/date
PATH=/bin:_tmp  # control output

type -ap date
