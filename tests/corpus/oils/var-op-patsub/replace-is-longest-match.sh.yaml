name: Replace is longest match
dialect: bash
source: oils:spec/var-op-patsub#replace-is-longest-match
stdout: "begin [] end\n"
---
# If it were shortest, then you would just replace the first <html>
s='begin <html></html> end'
echo ${s/<*>/[]}
