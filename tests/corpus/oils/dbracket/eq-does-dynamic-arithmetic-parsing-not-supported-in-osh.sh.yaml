name: -eq does dynamic arithmetic parsing (not supported in OSH)
dialect: bash
source: 
  oils:spec/dbracket#eq-does-dynamic-arithmetic-parsing-not-supported-in-osh
stdout: "true\ntrue\n"
---
[[ 1+2 -eq 3 ]] && echo true
expr='1+2'
[[ $expr -eq 3 ]] && echo true  # must be dynamically parsed
