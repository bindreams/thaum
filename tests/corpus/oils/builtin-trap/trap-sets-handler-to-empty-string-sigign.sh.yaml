name: trap '' sets handler to empty string (SIG_IGN)
dialect: bash
source: oils:spec/builtin-trap#trap-sets-handler-to-empty-string-sigign
stdout: |-
  trap -- '' SIGUSR1
---

# Note: this doesn't actually test that it's SIG_IGN

trap '' USR1
trap
