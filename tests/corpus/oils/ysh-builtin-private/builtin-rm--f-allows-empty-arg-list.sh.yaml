disabled: true
name: builtin rm -f allows empty arg list
dialect: bash
source: oils:spec/ysh-builtin-private#builtin-rm--f-allows-empty-arg-list
stdout: "status=0\n"
---
case $SH in
  *osh) prefix='builtin' ;;
  *) prefix='' ;;
esac

$prefix rm -f
echo status=$?
