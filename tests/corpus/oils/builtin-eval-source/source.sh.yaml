name: Source
dialect: bash
source: oils:spec/builtin-eval-source#source
stdout: "libvar\n"
environment:
  TMP: /tmp
---
lib=$TMP/spec-test-lib.sh
echo 'LIBVAR=libvar' > $lib
. $lib  # dash doesn't have source
echo $LIBVAR
