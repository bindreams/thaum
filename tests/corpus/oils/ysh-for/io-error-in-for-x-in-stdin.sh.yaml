disabled: true
name: I/O error in for x in (stdin) {
dialect: bash
source: oils:spec/ysh-for#io-error-in-for-x-in-stdin
stdout: |-
  pass
---

set +o errexit

# EISDIR - stdin descriptor is dir
$[ENV.SH] -c 'for x in (io.stdin) { echo $x }' < /
if test $? -ne 0; then
  echo pass
fi
