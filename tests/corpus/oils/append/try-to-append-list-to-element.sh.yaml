name: Try to append list to element
dialect: bash
source: oils:spec/append#try-to-append-list-to-element
status: 2
stdout: ''
---
# bash - runtime error: cannot assign list to array number
# mksh - a[-1]+: is not an identifier
# osh - parse error -- could be better!
a=(1 '2 3')
a[-1]+=(4 5)
argv.py "${a[@]}"
