name: 'symbolic syntax error: b=rwx'
dialect: bash
source: oils:spec/builtin-umask#symbolic-syntax-error-brwx
stdout: |
  error
  0124
---
umask 0124

umask b=rwx
case $? in
  1) echo error ;;
  2) echo error ;;
  *) echo status=$? ;;
esac

umask  # make sure it hasn't changed
