name: Other flag parsers are not affected by - rule
dialect: bash
source: oils:spec/sh-usage#other-flag-parsers-are-not-affected-by---rule
stdout: |-
  reply=foo
---

# -c is special, with quit_parsing_flags

echo 'foo-bar' | { read -d -; echo reply=$REPLY; }
