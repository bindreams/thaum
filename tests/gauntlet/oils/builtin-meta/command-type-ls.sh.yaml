name: $(command type ls)
dialect: bash
source: oils:spec/builtin-meta#command-type-ls
stdout: |
  FUNCTION
  status=0
---
type() { echo FUNCTION; }
type
s=$(command type echo)
echo $s | grep builtin > /dev/null
echo status=$?
