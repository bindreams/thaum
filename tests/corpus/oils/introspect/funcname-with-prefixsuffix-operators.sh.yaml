name: ${FUNCNAME} with prefix/suffix operators
dialect: bash
source: oils:spec/introspect#funcname-with-prefixsuffix-operators
stdout: "['5']\n['c']\n['heck']\n"
---

check() {
  argv.py "${#FUNCNAME}"
  argv.py "${FUNCNAME::1}"
  argv.py "${FUNCNAME:1}"
}
check
