name: Register the same handler for multiple signals
dialect: bash
source: oils:spec/builtin-trap#register-the-same-handler-for-multiple-signals
stdout: |
  trap -- 'echo test' EXIT
  trap -- 'echo test' SIGINT
  trap -- 'echo test' SIGTERM
  test
---
trap 'echo test' TERM 2 EXIT
trap
