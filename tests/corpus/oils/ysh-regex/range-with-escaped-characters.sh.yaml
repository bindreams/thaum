disabled: true
name: Range with escaped characters
dialect: bash
source: oils:spec/ysh-regex#range-with-escaped-characters
stdout: " 5b 01 2d 0f 5d 0a\n"
---
shopt -s ysh:all

var pat = null

setvar pat = / [ \x01 - \x0f ] /
echo $pat | od -A n -t x1
