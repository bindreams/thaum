name: extglob empty pattern
dialect: bash
source: oils:spec/extglob-match#extglob-empty-pattern
stdout: |
  TRUE
  TRUE
  FALSE
  FALSE
---
shopt -s extglob
[[ '' == @() ]] && echo TRUE
[[ '' == @(||) ]] && echo TRUE
[[ X == @() ]] || echo FALSE
[[ '|' == @(||) ]] || echo FALSE
