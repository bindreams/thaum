name: Storing backslashes and then echoing them
dialect: bash
source: oils:spec/quote#storing-backslashes-and-then-echoing-them
stdout: "\\ \\\\\n\\ \\\\\n"
---
# This is a bug fix; it used to cause problems with unescaping.
one='\'
two='\\'
echo $one $two
echo "$one" "$two"
