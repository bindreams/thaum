disabled: true
name: hay builtin usage
dialect: bash
source: oils:spec/hay#hay-builtin-usage
stdout: |
  status=2
  status=0
  read 0
  greater 0
---

hay define
echo status=$?

hay define -- package user
echo status=$?

hay pp | wc -l | read n
echo read $?
test $n -gt 0
echo greater $?
