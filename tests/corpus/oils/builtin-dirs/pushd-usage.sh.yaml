name: pushd usage
dialect: bash
source: oils:spec/builtin-dirs#pushd-usage
stdout: |-
  status=2
  status=0
  status=0
---
pushd -z
echo status=$?
pushd /tmp >/dev/null
echo status=$?
pushd -- /tmp >/dev/null
echo status=$?
