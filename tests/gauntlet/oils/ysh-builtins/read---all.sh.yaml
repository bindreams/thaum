disabled: true
name: read --all
dialect: bash
source: oils:spec/ysh-builtins#read---all
stdout: |
  [foo
  ]
  [bad
  ]
---
echo foo | read --all
echo "[$_reply]"

echo bad > tmp.txt
read --all (&x) < tmp.txt
echo "[$x]"
