name: File redirects with glob args (bash and zsh only)
dialect: bash
source: oils:spec/redirect-multi#file-redirects-with-glob-args-bash-and-zsh-only
stdout: |
  hi
  escaped
---

touch one-bar

echo hi > one-*

cat one-bar

echo escaped > one-\*

cat one-\*
