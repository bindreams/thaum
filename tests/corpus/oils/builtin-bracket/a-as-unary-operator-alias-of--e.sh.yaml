name: -a as unary operator (alias of -e)
dialect: bash
source: oils:spec/builtin-bracket#a-as-unary-operator-alias-of--e
stdout: |-
  status=0
  status=1
---
# NOT IMPLEMENTED FOR OSH, but could be later.  See comment in core/id_kind.py.
[ -a / ]
echo status=$?
[ -a /nonexistent ]
echo status=$?
