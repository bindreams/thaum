name: trap ERR and errexit disabled context
dialect: bash
source: oils:spec/builtin-trap-err#trap-err-and-errexit-disabled-context
stdout: |
  line=3
  a
  b
  c
---

trap 'echo line=$LINENO' ERR

false
echo a

set -o errexit

echo b
if false; then
  echo xx
fi
echo c  # doesn't get here
