name: var x = $(echo bad; false) in OSH
dialect: bash
source: oils:spec/osh-bugs#var-x-echo-bad-false-in-osh
status: 1
stdout: "\n"
---

#shopt -s verbose_errexit

# This turns on command_sub_errexit and fails
var x = $(echo bad; false)
echo 'unreachable'

pp test_ (x)
