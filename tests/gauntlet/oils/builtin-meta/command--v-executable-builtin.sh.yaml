name: command -v executable, builtin
dialect: bash
source: oils:spec/builtin-meta#command--v-executable-builtin
stdout: |
  /grep
  /ls

  true
  eval
---

#command -v grep ls

command -v grep | egrep -o '/[^/]+$'
command -v ls | egrep -o '/[^/]+$'
echo

command -v true
command -v eval
