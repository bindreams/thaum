name: Sourcing a script that returns at the top level
dialect: bash
source: oils:spec/strict-options#sourcing-a-script-that-returns-at-the-top-level
stdout: |
  one
  return-helper.sh
  42
  two
---
echo one
. $REPO_ROOT/spec/testdata/return-helper.sh
echo $?
echo two
