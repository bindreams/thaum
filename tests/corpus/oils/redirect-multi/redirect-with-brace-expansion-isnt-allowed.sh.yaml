name: Redirect with brace expansion isn't allowed
dialect: bash
source: oils:spec/redirect-multi#redirect-with-brace-expansion-isnt-allowed
stdout: "status=1\nstatus=1\n"
---

echo hi > a-{one,two}
echo status=$?

head a-*
echo status=$?
