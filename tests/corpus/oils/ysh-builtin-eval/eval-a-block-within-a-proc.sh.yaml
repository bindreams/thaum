name: Eval a block within a proc
dialect: bash
source: oils:spec/ysh-builtin-eval#eval-a-block-within-a-proc
stdout: "In a block!\n"
---
proc run (;;; block) {
  call io->eval(block)
}

run {
  echo 'In a block!'
}
