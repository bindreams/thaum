name: no recursive arith [ 1+2 -eq 3 ]
dialect: bash
source: oils:spec/builtin-bracket#no-recursive-arith-12--eq-3
stdout: "status=2\nstatus=2\n"
---

[ 1+2 -eq 3 ]
echo status=$?

s='1+2'
[ "$s" -eq 3 ]
echo status=$?
