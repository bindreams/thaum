name: bind -r of bind -x commands
dialect: bash
source: oils:spec/builtin-bind#bind--r-of-bind--x-commands
stdout: |
  status=1
  \C-o\C-s\C-h
  status=0
  status=1
---
bind -X | grep -oF '\C-o\C-s\C-h'
echo status=$?

bind -x '"\C-o\C-s\C-h": echo foo'
bind -X | grep -oF '\C-o\C-s\C-h'
echo status=$?

bind -r "\C-o\C-s\C-h"
bind -X | grep -oF '\C-o\C-s\C-h'
echo status=$?
