name: Matching literal '@(cc)'
dialect: bash
source: oils:spec/extglob-match#matching-literal-cc
stdout: |-
  status=0
  status=1
  status=0
  status=1
---

# extglob is OFF.  Doesn't affect bash or mksh!
[[ cc == @(cc) ]] 
echo status=$?
[[ cc == '@(cc)' ]]
echo status=$?

shopt -s extglob

[[ cc == @(cc) ]]
echo status=$?
[[ cc == '@(cc)' ]]
echo status=$?
