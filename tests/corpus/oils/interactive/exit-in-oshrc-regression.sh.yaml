name: "'exit' in oshrc (regression)"
dialect: bash
source: oils:spec/interactive#exit-in-oshrc-regression
status: 42
stdout: "one\n"
---
cat >$TMP/oshrc <<EOF
echo one
exit 42
echo two
EOF
$SH --rcfile $TMP/oshrc -i -c 'echo hello'
