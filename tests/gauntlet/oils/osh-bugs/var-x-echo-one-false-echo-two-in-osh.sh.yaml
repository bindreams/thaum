name: var x = $(echo one; false; echo two) in OSH
dialect: bash
source: oils:spec/osh-bugs#var-x-echo-one-false-echo-two-in-osh
status: 1
stdout: "\n"
---

#shopt -s verbose_errexit

# I don't understand why this doesn't fail
var x = $(echo one; false; echo two)
echo 'unreachable'

pp test_ (x)
