name: 'Regression: Array length after unset'
dialect: bash
source: oils:spec/array#regression-array-length-after-unset
stdout: "len 2;\nlen 1;\nlast x;\n"
---
case $SH in mksh) exit ;; esac

a=(x)
a[9]=y
echo "len ${#a[@]};"

unset -v 'a[-1]'
echo "len ${#a[@]};"
echo "last ${a[@]: -1};"
