name: Slice string with invalid UTF-8 with strict_word_eval
dialect: bash
source: 
  oils:spec/var-op-slice#slice-string-with-invalid-utf-8-with-strictwordeval
status: 1
---
shopt -s strict_word_eval || true
echo slice
s=$(echo -e "\xFF")bcdef
echo -${s:1:3}-
slice
