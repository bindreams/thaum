name: test -p named pipe
dialect: bash
source: oils:spec/builtin-bracket#test--p-named-pipe
stdout: |
  status=0
  status=1
environment:
  TMP: /tmp
---
mkfifo $TMP/fifo
test -p $TMP/fifo
echo status=$?

test -p testdata
echo status=$?
