name: Assigning whole raray (( b = a ))
dialect: bash
source: oils:spec/dparen#assigning-whole-raray-b-a
status: 0
stdout: |
  4 5 6
  4
---
a=(4 5 6)
(( b = a ))

echo "${a[@]}"

# OSH doesn't like this
echo "${b[@]}"
