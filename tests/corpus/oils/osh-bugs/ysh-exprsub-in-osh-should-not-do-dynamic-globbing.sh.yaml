name: YSH $[expr_sub] in OSH should not do dynamic globbing
dialect: bash
source: oils:spec/osh-bugs#ysh-exprsub-in-osh-should-not-do-dynamic-globbing
stdout: "*.txt\n"
---

touch {foo,bar}.txt

shopt -s parse_ysh_expr_sub
echo $["*.txt"]
