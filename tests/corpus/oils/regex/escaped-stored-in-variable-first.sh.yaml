name: Escaped { stored in variable first
dialect: bash
source: oils:spec/regex#escaped-stored-in-variable-first
stdout: "['$PA', '$', 'PA']\n"
---
# from bash-completion
pat='^(\$\{?)([A-Za-z0-9_]*)$'
[[ '$PA' =~ $pat ]] && argv.py "${BASH_REMATCH[@]}"
