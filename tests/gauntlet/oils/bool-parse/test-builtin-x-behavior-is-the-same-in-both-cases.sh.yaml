name: 'test builtin: ( x ) behavior is the same in both cases'
dialect: bash
source: oils:spec/bool-parse#test-builtin-x-behavior-is-the-same-in-both-cases
stdout: |
  status=0
  status=0
---

test '(' x ')'
echo status=$?

test 0 -eq 0 -a '(' x ')'
echo status=$?
