name: strip unquoted and quoted ?
dialect: bash
source: oils:spec/var-op-strip#strip-unquoted-and-quoted
stdout: |
  foo]
  [foo]
  foo]
  [foo]
---
var='[foo]'
echo ${var#?}
echo ${var#"?"}
echo "${var#?}"
echo "${var#"?"}"
