name: extglob inside arg word
dialect: bash
source: oils:spec/extglob-match#extglob-inside-arg-word
stdout: |-
  rhs
  rhs arg
  nope
---
shopt -s extglob
[[ foo == @(foo|bar) ]] && echo rhs
[[ foo == ${unset:-@(foo|bar)} ]] && echo 'rhs arg'
[[ fo == ${unset:-@(foo|bar)} ]] || echo nope
