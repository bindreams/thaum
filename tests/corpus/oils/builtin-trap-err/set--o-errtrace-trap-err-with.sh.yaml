name: 'set -o errtrace: trap ERR with &'
dialect: bash
source: oils:spec/builtin-trap-err#set--o-errtrace-trap-err-with
stdout: |-
  line=8
  async
---
case $SH in mksh) exit ;; esac

set -o errtrace
trap 'echo line=$LINENO' ERR

false & wait

{ false; echo async; } & wait
