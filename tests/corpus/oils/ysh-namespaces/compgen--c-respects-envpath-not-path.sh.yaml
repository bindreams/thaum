disabled: true
name: compgen -c respects ENV.PATH, not PATH
dialect: bash
source: oils:spec/ysh-namespaces#compgen--c-respects-envpath-not-path
stdout: |-
  ls
---

setglobal ENV.PATH = '/bin'

# ls is required by POSIX
compgen -c | grep -w 'ls'
