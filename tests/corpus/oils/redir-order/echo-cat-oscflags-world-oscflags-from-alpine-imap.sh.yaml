name: echo `cat OSCFLAGS` "world" > OSCFLAGS (from Alpine imap)
dialect: bash
source: oils:spec/redir-order#echo-cat-oscflags-world-oscflags-from-alpine-imap
stdout: |
  hello world
---

echo hello > OSCFLAGS
echo `cat OSCFLAGS` "world" > OSCFLAGS
#echo $(cat OSCFLAGS) "world" > OSCFLAGS
cat OSCFLAGS
