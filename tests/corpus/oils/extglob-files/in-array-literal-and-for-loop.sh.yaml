name: In Array Literal and for loop
dialect: bash
source: oils:spec/extglob-files#in-array-literal-and-for-loop
stdout: "bar.py\nfoo.py\n---\nzzz bar.py foo.py\n"
---
shopt -s extglob
mkdir -p eg11
cd eg11
touch {foo,bar,spam}.py
for x in @(fo*|bar).py; do
  echo $x
done

echo ---
declare -a A
A=(zzz @(fo*|bar).py)
echo "${A[@]}"
