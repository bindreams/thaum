name: 'command sub: last command fails but keeps going and exit code is 0'
dialect: bash
source: 
  oils:spec/errexit-osh#command-sub-last-command-fails-but-keeps-going-and-exit-code-is-0
stdout: |
  one
  status=0
---
set -o errexit
echo $(echo one; false)  # we lost the exit code
echo status=$?
