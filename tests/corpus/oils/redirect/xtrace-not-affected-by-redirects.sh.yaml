name: xtrace not affected by redirects
dialect: bash
source: oils:spec/redirect#xtrace-not-affected-by-redirects
stdout: "\n"
stderr: |
  + printf aaaa
  + set +x
---
set -x
printf 'aaaa' > /dev/null 2> test_osh
set +x
cat test_osh
