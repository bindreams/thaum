name: PS1 evaluation order 2
dialect: bash
source: oils:spec/prompt#ps1-evaluation-order-2
stdout: "status=0\n"
environment:
  TMP: /tmp
---
foo=foo_value
dir=$TMP/'$foo'  # Directory name with a dollar!
mkdir -p $dir
cd $dir
PS1='\w $foo'
test "${PS1@P}" = "$PWD foo_value"
echo status=$?
