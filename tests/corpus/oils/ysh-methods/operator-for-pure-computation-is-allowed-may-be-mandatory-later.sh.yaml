disabled: true
name: => operator for pure computation is allowed (may be mandatory later)
dialect: bash
source: 
  oils:spec/ysh-methods#operator-for-pure-computation-is-allowed-may-be-mandatory-later
stdout: |
  yes
  ok
---

# later we may make it mandatory

if ("abc" => startsWith("a")) {
  echo yes
}

var mylist = [1, 2, 3]

# This one should be ->
call mylist->pop()
echo 'ok'
