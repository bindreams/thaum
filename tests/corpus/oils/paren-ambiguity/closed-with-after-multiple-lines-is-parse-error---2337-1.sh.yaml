name: '$(( closed with )) after multiple lines is parse error - #2337'
dialect: bash
source: 
  oils:spec/paren-ambiguity#closed-with-after-multiple-lines-is-parse-error---2337
stdout: "ok\n"
---

$SH -c '
echo $(( echo 1
echo 2
(( x ))
: $(( x ))
echo 3
))
'
if test $? -ne 0; then
  echo ok
fi
