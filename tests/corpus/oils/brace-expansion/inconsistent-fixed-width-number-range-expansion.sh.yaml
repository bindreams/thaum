name: Inconsistent fixed width number range expansion
dialect: bash
source: 
  oils:spec/brace-expansion#inconsistent-fixed-width-number-range-expansion
stdout: |-
  -001- -002- -003-
---
# zsh uses the first one, bash uses the max width?
echo -{01..003}-
