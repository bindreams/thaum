name: typeset -f prints function source code
dialect: bash
source: oils:spec/print-source-code#typeset--f-prints-function-source-code
stdout: |
  serialized
---
: prefix; myfunc() { echo serialized; }

code=$(typeset -f myfunc)

$SH -c "$code; myfunc"
