disabled: true
name: maybe() on invalid type is fatal error
dialect: bash
source: oils:spec/ysh-func-builtin#maybe-on-invalid-type-is-fatal-error
status: 3
stdout: "\n"
setup: |-
  #!/bin/sh
  cat > argv.py <<'HELPER'
  #!/usr/bin/env python3
  import sys
  print(sys.argv[1:])
  HELPER
  chmod +x argv.py
---

# not allowed
setvar marray = :||
argv.py a @[maybe(marray)] b
echo done
