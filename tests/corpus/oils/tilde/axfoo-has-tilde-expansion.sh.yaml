name: a[x]=foo:~ has tilde expansion
dialect: bash
source: oils:spec/tilde#axfoo-has-tilde-expansion
stdout: |-
  foo:/home/bar
  foo:/home/bar
---
case $SH in dash|zsh) exit ;; esac

HOME=/home/bar
declare -a a
a[0]=foo:~
echo ${a[0]}

declare -A A
A['x']=foo:~
echo ${A['x']}
