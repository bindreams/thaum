name: 'Out of range string slice: begin'
dialect: bash
source: oils:spec/var-op-slice#out-of-range-string-slice-begin
stdout: "_\n0\n"
---
# out of range begin doesn't raise error in bash, but in mksh it skips the
# whole thing!
foo=abcdefg
echo _${foo:100:3}
echo $?
