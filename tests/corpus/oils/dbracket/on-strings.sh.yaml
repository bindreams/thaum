name: '> on strings'
dialect: bash
source: oils:spec/dbracket#on-strings
stdout: "true\nfalse\n"
---
# NOTE: < doesn't need space, even though == does?  That's silly.
[[ b>a ]] && echo true
[[ b<a ]] || echo false
