name: pattern $f(x)  -- regression
dialect: bash
source: oils:spec/regex#pattern-fx----regression
stdout: |
  status=0
  status=1
---
f=fff
[[ fffx =~ $f(x) ]]
echo status=$?
[[ ffx =~ $f(x) ]]
echo status=$?
