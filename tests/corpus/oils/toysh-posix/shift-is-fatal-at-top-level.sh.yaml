name: shift is fatal at top level?
dialect: bash
source: oils:spec/toysh-posix#shift-is-fatal-at-top-level
stdout: |
  hello
---
# http://landley.net/notes.html#08-04-2020

# This makes a difference for zsh, but not for bash?
#set -o posix

$SH -c 'shift; echo hello'
