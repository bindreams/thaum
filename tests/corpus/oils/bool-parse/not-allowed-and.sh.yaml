name: 'Not allowed: [[ ) ]] and [[ ( ]]'
dialect: bash
source: oils:spec/bool-parse#not-allowed-and
status: 2
stdout: "\n"
parse-error: "unexpected token ')', expected a word in test expression"
---

[[ ) ]]
echo status=$?
[[ ( ]]
echo status=$?
