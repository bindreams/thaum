name: IFS='\ ' and backslash escaping
dialect: bash
source: oils:spec/builtin-read#ifs-and-backslash-escaping
stdout: |
  ['hello world', 'test']
  ['hello world  test', '']
setup: |-
  #!/bin/sh
  cat > argv.py <<'HELPER'
  #!/usr/bin/env python3
  import sys
  print(sys.argv[1:])
  HELPER
  chmod +x argv.py
---
IFS='\ '
echo "hello\ world  test" | (read a b; argv.py "$a" "$b")
IFS='\'
echo "hello\ world  test" | (read a b; argv.py "$a" "$b")
# In mksh/zsh, IFS='\' is stronger than backslash escaping
