name: $LINENO in "bare" redirect arg (bug regression)
dialect: bash
source: oils:spec/vars-special#lineno-in-bare-redirect-arg-bug-regression
environment:
  TMP: /tmp
---
filename=$TMP/bare3
rm -f $filename
> $TMP/bare$LINENO
test -f $filename && echo written
echo $LINENO
written
5
