name: cd away from dir that was deleted
dialect: bash
source: oils:spec/builtin-cd#cd-away-from-dir-that-was-deleted
stdout: |
  cd-nonexistent
  status=0
environment:
  TMP: /tmp
---
dir=$TMP/cd-nonexistent
mkdir -p $dir
cd $dir
rmdir $dir
cd $TMP
echo $(basename $OLDPWD)
echo status=$?
