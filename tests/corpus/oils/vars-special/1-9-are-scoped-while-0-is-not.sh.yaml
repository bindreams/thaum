name: $1 .. $9 are scoped, while $0 is not
dialect: bash
source: oils:spec/vars-special#1-9-are-scoped-while-0-is-not
stdout: |-
  sh
  a b
---
fun() {
  case $0 in
    *sh)
      echo 'sh'
      ;;
    *sh-*)  # bash-4.4 is OK
      echo 'sh'
      ;;
  esac

  echo $1 $2
}
fun a b
