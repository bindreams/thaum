name: empty argv
dialect: bash
source: oils:spec/word-split#empty-argv
stdout: "['1', '2', '3', '', '4', '5']\n"
setup: |
  #!/bin/sh
  cat > argv.py <<'HELPER'
  #!/usr/bin/env python3
  import sys
  print(sys.argv[1:])
  HELPER
  chmod +x argv.py

---
argv.py 1 "$@" 2 $@ 3 "$*" 4 $* 5
