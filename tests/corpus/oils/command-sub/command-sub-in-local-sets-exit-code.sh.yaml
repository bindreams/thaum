name: Command Sub in local sets exit code
dialect: bash
source: oils:spec/command-sub#command-sub-in-local-sets-exit-code
stdout: "x\n0\n0\n"
---
# A command resets the exit code, but an assignment doesn't.
f() {
  echo $(echo x; exit 33)
  echo $?
  local x=$(echo x; exit 33)
  echo $?
}
f
