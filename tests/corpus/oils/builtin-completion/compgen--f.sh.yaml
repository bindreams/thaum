name: compgen -f
dialect: bash
source: oils:spec/builtin-completion#compgen--f
stdout: |
  one
  three
  two
  --
  three
  two
environment:
  TMP: /tmp
---
mkdir -p $TMP/compgen
touch $TMP/compgen/{one,two,three}
cd $TMP/compgen
compgen -f | sort
echo --
compgen -f t | sort
