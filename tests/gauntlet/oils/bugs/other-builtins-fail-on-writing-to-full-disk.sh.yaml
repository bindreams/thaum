name: other builtins fail on writing to full disk
dialect: bash
source: oils:spec/bugs#other-builtins-fail-on-writing-to-full-disk
stdout: |
  status=1
  status=1
---

type echo > /dev/full
echo status=$?

# other random builtin
ulimit -a > /dev/full
echo status=$?
