name: Empty alternative with empty string suffix
dialect: bash
source: oils:spec/brace-expansion#empty-alternative-with-empty-string-suffix
status: 0
stdout: "['X', '', 'Y', '']\n"
---
# zsh and mksh don't do word elision, probably because they do brace expansion
# AFTER variable substitution.
argv.py {X,,Y,}''
