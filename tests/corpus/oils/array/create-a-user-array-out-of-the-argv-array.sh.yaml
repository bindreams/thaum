name: Create a "user" array out of the argv array
dialect: bash
source: oils:spec/array#create-a-user-array-out-of-the-argv-array
stdout: "['x y', 'z', 'a b', 'c']\n"
---
set -- 'a b' 'c'
array1=('x y' 'z')
array2=("$@")
argv.py "${array1[@]}" "${array2[@]}"
