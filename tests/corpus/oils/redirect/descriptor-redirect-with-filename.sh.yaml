name: Descriptor redirect with filename
dialect: bash
source: oils:spec/redirect#descriptor-redirect-with-filename
stdout: "status=1\n"
---
# bash/mksh treat this like a filename, not a descriptor.
# dash aborts.
echo one 1>&$TMP/nonexistent-filename__
echo "status=$?"
