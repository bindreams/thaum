name: nonexistent --rcdir is ignored
dialect: bash
source: oils:spec/interactive#nonexistent---rcdir-is-ignored
stdout: |
  hi
  status=0
environment:
  TMP: /tmp
---
case $SH in bash) exit ;; esac

$SH --rcdir $TMP/__does-not-exist -i -c 'echo hi'
echo status=$?
