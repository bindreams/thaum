name: 'OLD: command sub in redirect in conditional'
dialect: bash
source: oils:spec/errexit-osh#old-command-sub-in-redirect-in-conditional
stdout: "2\ntmp_contents\n"
---
set -o errexit

if echo tmp_contents > $(echo tmp); then
  echo 2
fi
cat tmp
