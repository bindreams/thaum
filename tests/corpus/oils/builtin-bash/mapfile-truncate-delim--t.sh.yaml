name: 'mapfile (truncate delim): -t'
dialect: bash
source: oils:spec/builtin-bash#mapfile-truncate-delim--t
stdout: "n=3\n[1]\n[3]\n[5]\n"
---
type mapfile >/dev/null 2>&1 || exit 0
printf '%s\n' {1..5..2} | {
  mapfile -t arr
  echo "n=${#arr[@]}"
  printf '[%s]\n' "${arr[@]}"
}
