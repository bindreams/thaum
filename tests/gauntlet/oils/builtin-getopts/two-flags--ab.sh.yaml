name: 'two flags: -ab'
dialect: bash
source: oils:spec/builtin-getopts#two-flags--ab
stdout: |
  OPTIND=1 opt=a OPTARG=
  OPTIND=2 opt=b OPTARG=
---
getopts "ab" opt -ab
echo OPTIND=$OPTIND opt=$opt OPTARG=$OPTARG
getopts "ab" opt -ab
echo OPTIND=$OPTIND opt=$opt OPTARG=$OPTARG
