name: Double quoting pat variable -- again bash doesn't like it.
dialect: bash
source: oils:spec/regex#double-quoting-pat-variable----again-bash-doesnt-like-it
status: 1
stdout: ''
---
pat="^(a b)$"
[[ 'a b' =~ "$pat" ]] && echo true
