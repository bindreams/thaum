name: Invalid LValue
dialect: bash
source: oils:spec/arith#invalid-lvalue
status: 2
stdout: ''
parse-error: "unexpected token assignment target must be a variable, expected a valid arithmetic expression"
---
a=9
(( (a + 2) = 3 ))
echo $a
#   dash doesn't implement assignment
