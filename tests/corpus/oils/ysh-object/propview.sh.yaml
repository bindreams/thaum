disabled: true
name: propView()
dialect: bash
source: oils:spec/ysh-object#propview
status: 3
stdout: "(Dict)   {\"x\":3,\"y\":4}\n(Dict)   {\"x\":99,\"y\":4}\n"
---

var obj = Object(null, {x: 3, y: 4})
var props = propView(obj)

pp test_ (props)

# object can be mutated
setvar props.x = 99

pp test_ (props)

var e = propView(null)  # error
