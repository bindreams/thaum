name: dynamic declare instead of %q
dialect: bash
source: oils:spec/builtin-printf#dynamic-declare-instead-of-q
stdout: |
  '"quoted" with spaces and \'
---
var=foo
val='"quoted" with spaces and \'
# I think this is bash 4.4 only.
declare $var="${val@Q}"
echo "$foo"
