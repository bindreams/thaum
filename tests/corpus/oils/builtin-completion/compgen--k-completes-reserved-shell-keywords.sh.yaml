name: compgen -k completes reserved shell keywords
dialect: bash
source: 
  oils:spec/builtin-completion#compgen--k-completes-reserved-shell-keywords
stdout: |-
  do
  done
  status=0
  elif
  else
  status=0
---
compgen -k do | sort
echo status=$?
compgen -k el | sort
echo status=$?
