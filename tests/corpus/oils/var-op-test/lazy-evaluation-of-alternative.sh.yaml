name: Lazy Evaluation of Alternative
dialect: bash
source: oils:spec/var-op-test#lazy-evaluation-of-alternative
status: 0
stdout: |-
  x
  0
  0
  1
---
i=0
x=x
echo ${x:-$((i++))}
echo $i
echo ${undefined:-$((i++))}
echo $i  # i is one because the alternative was only evaluated once
