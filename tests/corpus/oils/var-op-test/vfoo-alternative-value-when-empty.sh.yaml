name: ${v:+foo} Alternative value when empty
dialect: bash
source: oils:spec/var-op-test#vfoo-alternative-value-when-empty
stdout: |-
  v is not empty
---
v=foo
empty=''
echo ${v:+v is not empty} ${empty:+is not empty}
