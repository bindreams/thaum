name: history -c clears in-memory history
dialect: bash
source: oils:spec/builtin-history#history--c-clears-in-memory-history
stdout: "1\n^D\n"
---

$SH --norc -i <<'EOF'
echo 'foo' > /dev/null
echo 'bar' > /dev/null
history -c 
history | wc -l
EOF

case $SH in bash) echo '^D' ;; esac
