disabled: true
name: 'Parsing crash - bug #2003'
dialect: bash
source: oils:spec/ysh-bugs#parsing-crash---bug-2003
stdout: |-
  status=2
  status=2
---

set +o errexit

$[ENV.SH] -c 'proc y (;x) { return = x }'
echo status=$?

$[ENV.SH] -c 'func y (;x) { return = x }'
echo status=$?
