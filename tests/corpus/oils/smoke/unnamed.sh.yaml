name: '"$@" "$*"'
dialect: bash
source: oils:spec/smoke#unnamed
stdout: |
  ['a b', 'c d', 'a b c d']
setup: |-
  #!/bin/sh
  cat > argv.py <<'HELPER'
  #!/usr/bin/env python3
  import sys
  print(sys.argv[1:])
  HELPER
  chmod +x argv.py
---
fun () {
  argv.py "$@" "$*"
}
fun "a b" "c d"
