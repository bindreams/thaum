name: "[[ isn't allowed in ysh"
dialect: bash
source: oils:spec/ysh-options#isnt-allowed-in-ysh
status: 2
stdout: "status=0\nstatus=0\n"
---
[[ 3 == 3 ]]
echo status=$?

shopt -s ysh:upgrade
[[ 3 == 3 ]]
echo status=$?

shopt -s ysh:all
[[ 3 == 3 ]]
echo status=$?

[[ 0 == 0 ]]
echo status=$?
