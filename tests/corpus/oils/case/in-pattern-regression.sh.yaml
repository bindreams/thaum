name: \(\) in pattern (regression)
dialect: bash
source: oils:spec/case#in-pattern-regression
stdout: |
  match
  extglob
---
s='foo()'

case $s in
  *\(\)) echo 'match'
esac

case $SH in dash) exit ;; esac  # not implemented

shopt -s extglob

case $s in
  *(foo|bar)'()') echo 'extglob'
esac
