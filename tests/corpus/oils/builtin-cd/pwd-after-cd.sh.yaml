name: pwd after cd ..
dialect: bash
source: oils:spec/builtin-cd#pwd-after-cd
stdout: |
  dir-two
  dir-one
environment:
  TMP: /tmp
---
dir=$TMP/dir-one/dir-two
mkdir -p $dir
cd $dir
echo $(basename $(pwd))
cd ..
echo $(basename $(pwd))
