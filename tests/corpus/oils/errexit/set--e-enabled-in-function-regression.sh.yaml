name: set -e enabled in function (regression)
dialect: bash
source: oils:spec/errexit#set--e-enabled-in-function-regression
stdout: |-
  should be executed
  should be executed
---
foo() {
  set -e
  false
  echo "should be executed"
}
#foo && true
#foo || true

if foo; then
  true
fi

echo "should be executed"
