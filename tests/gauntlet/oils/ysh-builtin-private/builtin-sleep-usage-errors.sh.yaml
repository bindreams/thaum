disabled: true
name: builtin sleep usage errors
dialect: bash
source: oils:spec/ysh-builtin-private#builtin-sleep-usage-errors
stdout: |
  status=2
  status=2
---
case $SH in bash) exit ;; esac

builtin sleep 0.5s
echo status=$?

builtin sleep 0.1 extra
echo status=$?
