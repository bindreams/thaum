name: $'' allowed within VarSub arguments
dialect: bash
source: oils:spec/var-sub-quote#allowed-within-varsub-arguments
stdout: "a\na\n"
---
# Odd behavior of bash/mksh: $'' is recognized but NOT ''!
x=abc
echo ${x%$'b'*}
echo "${x%$'b'*}"  # git-prompt.sh relies on this
