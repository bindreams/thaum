disabled: true
name: Errors for parameterized types
dialect: bash
source: oils:spec/ysh-builtin-meta#errors-for-parameterized-types
stdout: |-
  3
---
shopt -s ysh:upgrade

# more in test/ysh-runtime-errors.sh test-obj-methods
try {
  pp test_ (Bool[Str])
}
echo $[_error.code]

# I think this means
# TODO: need very low precedence operation
#
# Func[Int, Str : Int]
# Func[Int, Str -> Int]
# Func[Int, Str --> Int]
