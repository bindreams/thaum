name: Runtime errors for compgen -F func
dialect: bash
source: oils:spec/builtin-completion#runtime-errors-for-compgen--f-func
stdout: |
  status=1
---
_foo() {
  COMPREPLY=( foo bar )
  COMPREPLY+=( $(( 1 / 0 )) )  # FATAL, but we still have candidates
}
compgen -F _foo foo
echo status=$?
