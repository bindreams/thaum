name: '[[ equality matching'
dialect: bash
source: oils:spec/nocasematch-match#equality-matching
stdout: |-
  0
  0
  0
  0
---
shopt -s nocasematch
[[ a == A ]]; echo $?
[[ A == a ]]; echo $?
[[ A == [a] ]]; echo $?
[[ a == [A] ]]; echo $?
