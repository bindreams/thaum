[package]
name = "thaum-testkit"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
thaum = { path = "../.." }
serde = { version = "1", features = ["derive"] }
serde_yaml2 = "0.1"
regex = "1"
skuld = { git = "https://github.com/bindreams/skuld" }

[lib]
harness = false
