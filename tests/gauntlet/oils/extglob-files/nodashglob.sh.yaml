name: no_dash_glob
dialect: bash
source: oils:spec/extglob-files#nodashglob
stdout: |
  -dash bar foo
  bar foo
---
shopt -s extglob
mkdir -p opts
cd opts

touch -- foo bar -dash
echo @(*)

shopt --set no_dash_glob
echo @(*)
