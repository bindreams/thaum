disabled: true
name: $"foo $x" to make "foo $x" explicit
dialect: bash
source: oils:spec/ysh-string#foo-x-to-make-foo-x-explicit
stdout: |-
  foo bar
  equal
---

var x = $"bar"

# expression mode
var y = $"foo $x"
echo "$y"

# command mode
if test "$y" = $"foo $x"; then
  echo equal
fi
