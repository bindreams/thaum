disabled: true
name: Range
dialect: bash
source: oils:spec/ysh-printing#range
stdout: |
  (Range 1 ..< 100)
  (Dict)  {k: (Range 1 ..< 100)}

  <Range>
  (Dict)   {"k":<Range>}
---
var x = 1..<100

pp value (x)

# TODO: show type here, like (Range 1 ..< 100)

pp value ({k: x})

echo

pp test_ (x)
pp test_ ({k: x})
