name: lookup with double quoted strings "mykey"
dialect: bash
source: oils:spec/array-assoc#lookup-with-double-quoted-strings-mykey
stdout: |-
  b bar c
---
declare -A a
a["aa"]=b
a["foo"]=bar
a['a+1']=c
echo "${a["aa"]}" "${a["foo"]}" "${a["a+1"]}"
