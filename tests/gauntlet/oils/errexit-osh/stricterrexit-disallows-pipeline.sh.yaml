name: strict_errexit disallows pipeline
dialect: bash
source: oils:spec/errexit-osh#stricterrexit-disallows-pipeline
status: 1
---
set -o errexit
shopt -s strict_errexit || true

if echo 1 | grep 1; then
  echo one
fi
