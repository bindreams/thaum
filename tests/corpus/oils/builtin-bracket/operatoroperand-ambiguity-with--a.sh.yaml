name: operator/operand ambiguity with -a
dialect: bash
source: oils:spec/builtin-bracket#operatoroperand-ambiguity-with--a
stdout: |-
  status=0
---
# bash parses it as '-z' AND '-a'.  It's a syntax error in mksh but somehow a
# runtime error in dash.
[ -z -a -a ]
echo status=$?
