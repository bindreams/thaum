name: List => indexOf()
dialect: bash
source: oils:spec/ysh-methods#list-indexof
stdout: "-1\n0\n1\n3\n"
---
var items = [1, '2', 3, { 'a': 5 }]

json write (items => indexOf('a'))
json write (items => indexOf(1))
json write (items => indexOf('2'))
json write (items => indexOf({'a': 5}))
