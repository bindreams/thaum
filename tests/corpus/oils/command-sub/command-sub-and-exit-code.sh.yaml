name: Command Sub and exit code
dialect: bash
source: oils:spec/command-sub#command-sub-and-exit-code
stdout: |-
  x
  0
  33
---
# A command resets the exit code, but an assignment doesn't.
echo $(echo x; exit 33)
echo $?
x=$(echo x; exit 33)
echo $?
