name: Here doc with builtin 'read'
dialect: bash
source: oils:spec/here-doc#here-doc-with-builtin-read
stdout: "=val1= =val2=\n"
---
# read can't be run in a subshell.
read v1 v2 <<EOF
val1 val2
EOF
echo =$v1= =$v2=
