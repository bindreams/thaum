name: declare in an if statement
dialect: bash
source: oils:spec/assign#declare-in-an-if-statement
stdout: |
  bar
  eggs
---
# bug caught by my feature detection snippet in bash-completion
if ! foo=bar; then
  echo BAD
fi
echo $foo
if ! eval 'spam=eggs'; then
  echo BAD
fi
echo $spam
