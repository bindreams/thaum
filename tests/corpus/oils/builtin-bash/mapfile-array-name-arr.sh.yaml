name: 'mapfile (array name): arr'
dialect: bash
source: oils:spec/builtin-bash#mapfile-array-name-arr
stdout: "n=3\n[1\n]\n[3\n]\n[5\n]\n"
---
type mapfile >/dev/null 2>&1 || exit 0
printf '%s\n' {1..5..2} | {
  mapfile arr
  echo "n=${#arr[@]}"
  printf '[%s]\n' "${arr[@]}"
}
