name: autoconf arithmetic - relaxed eval_unsafe_arith (#1450)
dialect: bash
source: oils:spec/bugs#autoconf-arithmetic---relaxed-evalunsafearith-1450
stdout: |-
  2
---

as_fn_arith ()
{
    as_val=$(( $* ))
}
as_fn_arith 1 + 1
echo $as_val
