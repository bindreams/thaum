disabled: true
name: global frame doesn't contain env vars
dialect: bash
source: oils:spec/ysh-namespaces#global-frame-doesnt-contain-env-vars
stdout: |-
  (List)   [0,1]
---

#pp frame_vars_

try {
  pp frame_vars_ | grep -o TMP
}
pp test_ (_pipeline_status)
