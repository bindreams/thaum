name: \0400 is one more than the highest octal byte
dialect: bash
source: oils:spec/builtin-echo#0400-is-one-more-than-the-highest-octal-byte
stdout: " 00 30\n"
---
# It is 256 % 256 which gets interpreted as a NUL byte.
echo -en '\04000' | od -A n -t x1 | sed 's/ \+/ /g'
