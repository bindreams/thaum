name: stop on errors
dialect: bash
source: oils:spec/ysh-case#stop-on-errors
status: 3
stdout: "\n"
---
var x = "42"
case (x) {
  (42 / 0) { echo no error }

  ('42') { echo a }
  42 { echo b }
  / '42' / { echo c }
  (Str(40 + 2)) { echo d }
}
