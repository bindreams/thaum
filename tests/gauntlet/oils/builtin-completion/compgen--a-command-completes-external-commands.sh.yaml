name: compgen -A command completes external commands
dialect: bash
source: 
  oils:spec/builtin-completion#compgen--a-command-completes-external-commands
stdout: |
  xargs
  status=0
---
# NOTE: this test isn't hermetic
compgen -A command xarg | uniq
echo status=$?
