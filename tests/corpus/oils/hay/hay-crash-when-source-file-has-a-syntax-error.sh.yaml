disabled: true
name: hay crash when source file has a syntax error
dialect: bash
source: oils:spec/hay#hay-crash-when-source-file-has-a-syntax-error
stdout: "OK\n"
---
shopt --set ysh:all

# crash.hay contains invalid hay
echo 'echo =' > crash.hay

try {
  const h = parseHay("crash.hay")
}
if (_error.code === 3) {
  echo OK  # parseHay should raise an error.Expr
}
