name: Glob unicode char
dialect: bash
source: oils:spec/glob#glob-unicode-char
stdout: |
  __a__ __μ__
environment:
  TMP: /tmp
---

touch $TMP/__a__
touch $TMP/__μ__
cd $TMP

echo __?__

# note: zsh also passes this, but it doesn't run with this file.
