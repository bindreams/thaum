name: cd passed block with return 1
dialect: bash
source: oils:spec/ysh-blocks#cd-passed-block-with-return-1
status: 1
stdout: "block\n"
---
shopt -s ysh:all

f() {
  cd / {
    echo block
    return 1
    echo 'not reached'
  }
}
f
echo 'not reached'
