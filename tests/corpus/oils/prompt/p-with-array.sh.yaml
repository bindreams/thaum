name: '@P with array'
dialect: bash
source: oils:spec/prompt#p-with-array
stdout: |
  a b c
  status=0
  a b c
  status=0
  x
  status=0
---
$SH -c 'echo ${@@P}' dummy a b c
echo status=$?
$SH -c 'echo ${*@P}' dummy a b c
echo status=$?
$SH -c 'a=(x y); echo ${a@P}' dummy a b c
echo status=$?
