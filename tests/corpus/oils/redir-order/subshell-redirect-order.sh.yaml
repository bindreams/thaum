name: subshell + redirect order
dialect: bash
source: oils:spec/redir-order#subshell-redirect-order
stdout: |-
  world
---

echo hello > OSCFLAGS
(echo `cat OSCFLAGS` "world") > OSCFLAGS
cat OSCFLAGS
