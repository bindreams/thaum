name: xtrace with unicode chars
dialect: bash
source: oils:spec/xtrace#xtrace-with-unicode-chars
stdout: "[μ] [μ]\n"
stderr: "+ echo '[μ]' '[μ]'\n"
---
case $SH in dash) exit ;; esac

mu1='[μ]'
mu2=$'[\u03bc]'

set -o xtrace
echo "$mu1" "$mu2"
