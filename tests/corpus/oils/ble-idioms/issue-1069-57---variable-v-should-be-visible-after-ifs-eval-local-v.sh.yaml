name: "Issue #1069 [57] - Variable v should be visible after IFS= eval 'local v=...'"
dialect: bash
source: 
  oils:spec/ble-idioms#issue-1069-57---variable-v-should-be-visible-after-ifs-eval-local-v
stdout: |-
  v=hello
---

set -u

f() {
  # The temp env messes it up
  IFS= eval "local v=\"\$*\""

  # Bug does not appear with only eval
  # eval "local v=\"\$*\""

  #declare -p v
  echo v=$v

  # test -v v; echo "v defined $?"
}

f h e l l o
