name: Match a literal with a glob character with a dynamic pattern
dialect: bash
source: 
  oils:spec/case#match-a-literal-with-a-glob-character-with-a-dynamic-pattern
stdout: "match\n"
---
x='b.py'
pat='[ab].py'
case "$x" in
  $pat) echo match ;;
esac
