name: Remove const suffix is vectorized on user array
dialect: bash
source: oils:spec/var-op-strip#remove-const-suffix-is-vectorized-on-user-array
status: 0
stdout: |
  ['1', '2', '3']
setup: |-
  #!/bin/sh
  cat > argv.py <<'HELPER'
  #!/usr/bin/env python3
  import sys
  print(sys.argv[1:])
  HELPER
  chmod +x argv.py
---
a=(1a 2a 3a)
argv.py ${a[@]%a}
