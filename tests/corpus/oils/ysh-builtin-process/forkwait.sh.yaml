disabled: true
name: forkwait
dialect: bash
source: oils:spec/ysh-builtin-process#forkwait
stdout: |
  hi
  status=42
  ok
---
shopt --set oil:upgrade
shopt --unset errexit

old=$PWD

forkwait {
  cd /
  echo hi
  exit 42
}
echo status=$?
if test "$old" = "$PWD"; then
  echo ok
fi
