name: NUL bytes with ${#s} (OSH and zsh agree)
dialect: bash
source: oils:spec/nul-bytes#nul-bytes-with-s-osh-and-zsh-agree
stdout: "empty=0\nnul=0\n"
---

case $SH in dash) exit ;; esac

empty=$''
nul=$'\0'

echo empty=${#empty}
echo nul=${#nul}
