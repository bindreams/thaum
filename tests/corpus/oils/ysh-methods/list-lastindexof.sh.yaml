disabled: true
name: List => lastIndexOf()
dialect: bash
source: oils:spec/ysh-methods#list-lastindexof
stdout: |-
  4
  3
  2
  -1
---
var items = ['a', 'b', 'c', 'b', 'a']

json write (items => lastIndexOf('a'))
json write (items => lastIndexOf('b'))
json write (items => lastIndexOf('c'))
json write (items => lastIndexOf('d'))
