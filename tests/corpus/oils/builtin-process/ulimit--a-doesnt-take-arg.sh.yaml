name: ulimit -a doesn't take arg
dialect: bash
source: oils:spec/builtin-process#ulimit--a-doesnt-take-arg
stdout: "failure that was expected\n"
---
case $SH in bash) exit ;; esac

ulimit -a 42
if test $? -ne 0; then
  echo 'failure that was expected'
fi
