name: ${!a[1]} is named ref in bash
dialect: bash
source: oils:spec/array#a1-is-named-ref-in-bash
status: 0
stdout: |
  ['bar']
setup: |-
  #!/bin/sh
  cat > argv.py <<'HELPER'
  #!/usr/bin/env python3
  import sys
  print(sys.argv[1:])
  HELPER
  chmod +x argv.py
---
# mksh ignores it
foo=bar
a=('1 2' foo '2 3')
argv.py "${!a[1]}"
