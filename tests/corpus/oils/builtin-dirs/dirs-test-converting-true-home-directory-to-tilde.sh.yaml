name: dirs test converting true home directory to tilde
dialect: bash
source: oils:spec/builtin-dirs#dirs-test-converting-true-home-directory-to-tilde
status: 0
stdout: |
  ~/oil_test/tmp /
---
cd /
HOME=/tmp
mkdir -p $HOME/oil_test/$HOME
pushd $HOME/oil_test/$HOME >/dev/null
dirs
