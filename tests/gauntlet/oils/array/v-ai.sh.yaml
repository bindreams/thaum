name: '[[ -v a[i] ]]'
dialect: bash
source: oils:spec/array#v-ai
stdout: |
  status=0
  status=1
---

# note: modern versions of zsh implement this

array=(1 2 3)
[[ -v array[1] ]]
echo status=$?

[[ -v array[4] ]]
echo status=$?
