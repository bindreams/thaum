name: assign to invalid ref
dialect: bash
source: oils:spec/nameref#assign-to-invalid-ref
stdout: |-
  ref=1
  ref=1
  ref=foo
---
ref=1   # mksh makes this READ-ONLY!  Because it's not valid.

echo ref=$ref
typeset -n ref
echo ref=$ref

ref=foo
echo ref=$ref
