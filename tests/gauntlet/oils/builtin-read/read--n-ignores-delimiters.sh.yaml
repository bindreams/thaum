name: read -N ignores delimiters
dialect: bash
source: oils:spec/builtin-read#read--n-ignores-delimiters
stdout: |
  a
  b
environment:
  TMP: /tmp
---
case $SH in dash|ash|zsh) exit ;; esac

echo $'a\nb\nc' > $TMP/read-lines.txt

read -N 3 out < $TMP/read-lines.txt
echo "$out"
