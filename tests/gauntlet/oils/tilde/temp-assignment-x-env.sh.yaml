name: temp assignment x=~ env
dialect: bash
source: oils:spec/tilde#temp-assignment-x-env
stdout: |
  xx=/home/bar
  xx=/root:/home/bar:/home/bar
---

HOME=/home/bar

xx=~ env | grep xx=

# Does it respect the colon rule too?
xx=~root:~:~ env | grep xx=
