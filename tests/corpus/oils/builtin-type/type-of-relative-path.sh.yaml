name: type of relative path
dialect: bash
source: oils:spec/builtin-type#type-of-relative-path
status: 1
stdout: |-
  _tmp/ex is _tmp/ex
---

mkdir -p _tmp
touch _tmp/file _tmp/ex
chmod +x _tmp/ex

type _tmp/file _tmp/ex

# dash and ash don't care if it's executable
# mksh
