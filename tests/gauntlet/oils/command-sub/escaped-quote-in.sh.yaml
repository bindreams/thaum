name: Escaped quote in [[ ]]
dialect: bash
source: oils:spec/command-sub#escaped-quote-in
stdout: |
  123  456
  "
environment:
  TMP: /tmp
---
file=$TMP/command-sub-dbracket
#rm -f $file
echo "123 `[[ $(echo \\" > $file) ]]` 456";
cat $file
