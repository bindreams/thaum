name: Arith sub with word parts
dialect: bash
source: oils:spec/arith#arith-sub-with-word-parts
stdout: "14\n"
---
# Making 13 from two different kinds of sub.  Geez.
echo $((1 + $(echo 1)${undefined:-3}))
